export interface TodoType {
  id: string;
  todo: string;
  isCompleted: boolean;
  createdAt: string;
}

export interface CreateTodoType {
  todo: string;
  isCompleted: boolean;
  createdAt: string;
}

